class Conta:
    def __init__(self, nome, numero, saldo)
        self.nome = nome
        self.numero = numero
        self.saldo = saldo

    def deposito()
        print(self.saldo + a)  
    def saque()
        print(self.saldo - a)
    def saldo()
        print(self.saldo)
class Conta_especial(Conta):
    super().__init__(self, limite)
        def saque()
        print(self.saldo - a)

class Conta_corrente(Conta):
    super().__init__(self, limite)
        pass

class Conta_poupanca(Conta):
    super().__init__(self, limite)
        pass

